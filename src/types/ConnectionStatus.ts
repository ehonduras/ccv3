export enum EVENT_CONNECTION_STATUS_NAME {
  CONNECTED = "connected",
  DISCONNECTED = "disconnected",
  RECONNECTING = "reconnecting",
  RECONNECTED = "reconnected",
  INCOMING_CALL = "incoming-call"
}
